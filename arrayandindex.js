const players = ["Peter", "Kate", "John"];
function arrayandindex(players) {
  // check the whole array
  for (let i = 0; i < players.length; i++) {
    //log the index (i) and the object
    console.log(i, players[i]);
  }
}
